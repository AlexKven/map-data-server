<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TripRecorder2.Views.TripPage"
             xmlns:maps="clr-namespace:Xamarin.Forms.GoogleMaps;assembly=Xamarin.Forms.GoogleMaps"
             Title="{Binding Title}">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Label Text="{Binding DisplayMessage}" HorizontalOptions="Center"/>
            <maps:Map Grid.Row="1" ItemsSource="{Binding MapPins, Mode=OneTime}" x:Name="MainMap"/>
            <Button Grid.Row="2" HorizontalOptions="Center"
                    Text="{Binding StartStopButtonText}"
                    Command="{Binding StartStopCommand}"
                    IsEnabled="{Binding IsStartStopButtonEnabled}"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>